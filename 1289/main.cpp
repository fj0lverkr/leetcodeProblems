#pragma once
#include <vector>

using namespace std;

class Solution {
private:
  vector<vector<int>> possFallingPaths;

public:
  int minFallingPathSum(vector<vector<int>> &grid) {
    int numRows = grid.size();
    int numCols = numRows;
    for (int i = 0; i < numRows; i++) {
    }
    return 0;
  }
};
